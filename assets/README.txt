Place the hero banner image here as:

    bannerBackground.png

This is the circuit-pattern navy graphic referenced by the hero on index.html
(see .hero in styles.css). Until the image is added, the hero falls back to a
solid navy (#332D56) background, so the site still renders correctly.
